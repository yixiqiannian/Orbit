# Orbit 热榜模块（Hotlist）实施计划

> **目标**: Orbit 新增「热榜」功能，第一期接入 GitHub Trending（每日），架构预留多数据源扩展。
> **技术栈**: FastAPI + SQLAlchemy + MySQL（后端）、Vue3 + Element Plus + Glass Morphism（前端）、httpx + BeautifulSoup4（抓取）

**架构**：`hotlist_items` 表存快照（source + date + rank 唯一约束）→ `services/github_trending.py` 抓取解析 GitHub Trending 页面 → `api/hotlist.py` 提供查询/抓取端点 → 前端 `Hotlist.vue` 展示（日期选择 + 数据源 tab）。

**已实测验证**（2026-08-13）：
- `https://github.com/trending?since=daily` 大陆网络可达（HTTP 200，~1.5s）
- 页面含 17 条 `article.Box-row`，解析选择器：
  - 仓库名: `h2 a.Link` → `href="/owner/repo"`，title 文本为 `owner / repo`
  - 描述: `p`（首段）
  - 语言: `span[itemprop="programmingLanguage"]`
  - 总 star: `a[href*="stargazers"]` 文本（如 "11,519"）
  - fork: `a[href*="forks"]` 文本（如 "728"）
  - 今日 star: `.f6` 区域文本 `Built by 2,855 stars today` → 正则提取
- 抓取必须带 UA header（`Mozilla/5.0 ...`），否则可能 403

## 任务依赖链

```
T1 DB模型 → T2 抓取服务 → T3 API路由 → T4 前端基建 → T5 页面 → T6 QA验收
```

## 通用约定（所有任务）

- **Python**: `G:\Orbit\backend\venv\Scripts\python.exe`（3.11，勿用系统 python）
- **启动后端**: `cd G:\Orbit\backend && venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000`
- **路由必须带尾斜杠**: `@router.get("/")`，否则 307 重定向
- **DB 连接**: 读 `backend/.env` 的 DB_* 配置（本地 MySQL）
- **UI 规范**: 前端改造前必读 `G:\Orbit\design-system\orbit\MASTER.md`（Glass Morphism：玻璃卡片 rgba(17,24,39,0.85) / blur(20px) / 圆角 16-40px / Primary #2563EB / 浅深双模式 data-theme）
- **API 认证**: 所有 /api/hotlist 端点需 `Depends(get_current_user)`（参照 reading.py）
- **git**: 每完成一个文件 commit 一次，消息 `feat(hotlist): xxx`，直接推 main

---

## T1: 数据库模型 + schema（database-engineer）

**Files**:
- Create: `G:\Orbit\backend\app\models\hotlist_item.py`
- Create: `G:\Orbit\backend\app\schemas\hotlist.py`
- Modify: `G:\Orbit\backend\app\models\__init__.py`
- Modify: `G:\Orbit\backend\scripts\init_db.py`

**hotlist_item.py**（参照 `app/models/book.py` 的 Base 写法）:
```python
from sqlalchemy import Column, Integer, String, Text, Date, DateTime, UniqueConstraint
from datetime import datetime
from app.core.database import Base

class HotlistItem(Base):
    __tablename__ = "hotlist_items"
    __table_args__ = (UniqueConstraint("source", "hot_date", "rank", name="uq_hotlist_source_date_rank"),)

    id = Column(Integer, primary_key=True, autoincrement=True)
    source = Column(String(50), nullable=False, default="github")   # 数据源标识
    rank = Column(Integer, nullable=False)                           # 排名
    title = Column(String(300), nullable=False)                      # 仓库全名
    url = Column(String(500), nullable=False)
    description = Column(Text, nullable=True)
    language = Column(String(50), nullable=True)
    stars_today = Column(Integer, nullable=True)                     # 今日新增 star
    stars_total = Column(Integer, nullable=True)
    forks = Column(Integer, nullable=True)
    hot_date = Column(Date, nullable=False)                          # 榜单日期
    created_at = Column(DateTime, default=datetime.utcnow)
```

**schemas/hotlist.py**（参照 reading.py 的 pydantic 风格）:
```python
from pydantic import BaseModel
from datetime import date, datetime
from typing import Optional

class HotlistItemOut(BaseModel):
    id: int
    source: str
    rank: int
    title: str
    url: str
    description: Optional[str] = None
    language: Optional[str] = None
    stars_today: Optional[int] = None
    stars_total: Optional[int] = None
    forks: Optional[int] = None
    hot_date: date
    created_at: datetime
    class Config:
        from_attributes = True

class HotlistResponse(BaseModel):
    date: date
    source: str
    items: list[HotlistItemOut]

class FetchResponse(BaseModel):
    message: str
    count: int
    date: date
```

**__init__.py**: 加入 `from app.models.hotlist_item import HotlistItem` 和 `__all__`。

**init_db.py**: import 加 `HotlistItem`。

**验收**:
1. `cd G:\Orbit\backend && venv\Scripts\python.exe scripts/init_db.py` 输出 `Database tables created successfully!`
2. MySQL 里 `DESCRIBE hotlist_items;` 显示 11 列，有唯一约束
3. `git add . && git commit -m "feat(hotlist): add HotlistItem model" && git push`

---

## T2: GitHub Trending 抓取服务（backend-engineer）

**Files**:
- Create: `G:\Orbit\backend\app\services\github_trending.py`

**要求**（参照 weread_client.py 的 httpx 用法，**不要用 requests**，requirements 里没有）:
```python
"""GitHub Trending 抓取服务."""
import re
from datetime import datetime
from bs4 import BeautifulSoup
import httpx

TRENDING_URL = "https://github.com/trending?since=daily"
HEADERS = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0 Safari/537.36",
    "Accept-Language": "en-US,en;q=0.9",
}

def _parse_num(text: str) -> int:
    """'11,519' -> 11519; '2,855' -> 2855; '-' -> 0"""
    t = text.strip().replace(",", "")
    if not t or t == "-":
        return 0
    try:
        return int(t)
    except ValueError:
        return 0

def _parse_today_stars(f6_text: str) -> int:
    """'HTML 11,519 728 Built by 2,855 stars today' -> 2855"""
    m = re.search(r"Built by\s+([\d,]+)\s+stars today", f6_text)
    return _parse_num(m.group(1)) if m else 0

def parse_trending(html: str) -> list[dict]:
    """解析 GitHub Trending 页面 HTML，返回条目 dict 列表。"""
    soup = BeautifulSoup(html, "html.parser")
    items = []
    for i, row in enumerate(soup.select("article.Box-row"), start=1):
        a = row.select_one("h2 a.Link")
        if not a:
            continue
        href = a.get("href", "")            # /owner/repo
        title = a.get_text(" ", strip=True)  # "owner / repo"
        desc_el = row.select_one("p")
        lang_el = row.select_one('span[itemprop="programmingLanguage"]')
        stars_el = row.select_one('a[href*="stargazers"]')
        forks_el = row.select_one('a[href*="forks"]')
        f6 = row.select_one(".f6")
        items.append({
            "rank": i,
            "title": title.replace(" / ", "/").strip(),
            "url": f"https://github.com{href}",
            "description": desc_el.get_text(strip=True) if desc_el else None,
            "language": lang_el.get_text(strip=True) if lang_el else None,
            "stars_total": _parse_num(stars_el.get_text()) if stars_el else 0,
            "forks": _parse_num(forks_el.get_text()) if forks_el else 0,
            "stars_today": _parse_today_stars(f6.get_text(" ", strip=True)) if f6 else 0,
        })
    return items

async def fetch_trending() -> list[dict]:
    """抓取并解析 GitHub Trending（每日）。失败时抛 httpx.HTTPError。"""
    async with httpx.AsyncClient(timeout=15, headers=HEADERS, follow_redirects=True) as client:
        resp = await client.get(TRENDING_URL)
        resp.raise_for_status()
        return parse_trending(resp.text)
```

**验收**:
1. 写临时脚本调用 `parse_trending`（用 G:/Hermes/trending.html 实测，该文件已存在）→ 输出 ≥10 条，字段完整（rank/title/url/description/language/stars_total/forks/stars_today）
2. `parse_num` 处理 "11,519" / "-" 边界正确
3. `fetch_trending` 实网调用成功返回 ≥10 条（如网络失败，至少 parse_trending 本地测试通过并注明）
4. commit `feat(hotlist): add github trending service`

---

## T3: Hotlist API 路由（backend-engineer）

**Files**:
- Create: `G:\Orbit\backend\app\api\hotlist.py`
- Modify: `G:\Orbit\backend\app\main.py`（注册 router）

**hotlist.py**（参照 reading.py 模式，prefix `/api/hotlist`，**所有路由带尾斜杠**）:

```python
"""热榜 API."""
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.orm import Session
from datetime import date, datetime
from typing import Optional

from app.core.database import get_db
from app.core.deps import get_current_user
from app.models.user import User
from app.models.hotlist_item import HotlistItem
from app.schemas.hotlist import HotlistResponse, HotlistItemOut, FetchResponse

router = APIRouter(prefix="/api/hotlist", tags=["热榜"])

# 数据源注册表（扩展新源时在这里加）
SOURCES = [
    {"key": "github", "name": "GitHub Trending", "url": "https://github.com/trending"},
]

@router.get("/sources/")
def list_sources():
    """列出可用数据源。"""
    return {"sources": SOURCES}

@router.get("/", response_model=HotlistResponse)
def get_hotlist(
    source: str = Query("github"),
    hot_date: Optional[date] = Query(None, description="榜单日期，默认今天"),
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """查询某日热榜。"""
    d = hot_date or date.today()
    items = (db.query(HotlistItem)
             .filter(HotlistItem.source == source, HotlistItem.hot_date == d)
             .order_by(HotlistItem.rank.asc())
             .all())
    return HotlistResponse(date=d, source=source, items=items)

@router.post("/fetch/", response_model=FetchResponse)
def fetch_hotlist(
    source: str = Query("github"),
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    """抓取指定源最新热榜并入库（按 source+date+rank 去重，已有数据跳过）。"""
    if source != "github":
        raise HTTPException(status_code=400, detail=f"暂不支持数据源: {source}")
    from app.services.github_trending import fetch_trending
    import asyncio
    try:
        items = asyncio.run(fetch_trending())
    except Exception as e:
        raise HTTPException(status_code=502, detail=f"抓取失败: {e}")
    today = date.today()
    added = 0
    for it in items:
        exists = (db.query(HotlistItem)
                  .filter(HotlistItem.source == source,
                          HotlistItem.hot_date == today,
                          HotlistItem.rank == it["rank"])
                  .first())
        if exists:
            continue
        db.add(HotlistItem(source=source, hot_date=today, **it))
        added += 1
    db.commit()
    return FetchResponse(message=f"抓取完成，新增 {added} 条", count=added, date=today)
```

**main.py**: `from app.api.hotlist import router as hotlist_router` + `app.include_router(hotlist_router)`。

**验收**（后端先启动，用 skill 的 JWT 生成法获取 token，curl 实测）:
1. `POST /api/hotlist/fetch/?source=github` → 200，count ≥ 10，真实仓库数据
2. `GET /api/hotlist/` → 200，返回当天 items（rank 升序）
3. `GET /api/hotlist/sources/` → 200，含 github
4. 再次 fetch 同一日期 → count=0（去重生效）
5. 未带 token → 401
6. commit `feat(hotlist): add hotlist API`

---

## T4: 前端 API 封装 + 路由 + 侧边栏（frontend-engineer）

**Files**:
- Create: `G:\Orbit\frontend\src\api\hotlist.ts`
- Modify: `G:\Orbit\frontend\src\router\index.ts`
- Modify: `G:\Orbit\frontend\src\components\Sidebar.vue`

**api/hotlist.ts**（参照 reading.ts）:
```typescript
import api from './index'

export interface HotlistItem {
  id: number
  source: string
  rank: number
  title: string
  url: string
  description?: string
  language?: string
  stars_today?: number
  stars_total?: number
  forks?: number
  hot_date: string
  created_at: string
}

export interface HotlistResponse {
  date: string
  source: string
  items: HotlistItem[]
}

export const hotlistApi = {
  list(params: { source?: string; hot_date?: string }) {
    return api.get<any, HotlistResponse>('/api/hotlist/', { params })
  },
  fetch(source: string = 'github') {
    return api.post<any, { message: string; count: number; date: string }>(`/api/hotlist/fetch/`, null, { params: { source } })
  },
  sources() {
    return api.get<any, { sources: { key: string; name: string; url: string }[] }>('/api/hotlist/sources/')
  }
}
```

**router/index.ts**: children 数组加（参照 /reading）:
```typescript
{ path: '/hotlist', name: 'Hotlist', component: () => import('../views/Hotlist.vue') }
```

**Sidebar.vue**: menuItems 加 `{ path: '/hotlist', name: 'Hotlist', icon: TrendCharts, title: '热榜' }`，import 加 `TrendCharts`（@element-plus/icons-vue 有该图标）。

**验收**:
1. `grep` 确认三处修改到位
2. `cd G:\Orbit\frontend && npx vite build` 成功（**不要用 npm run build**，会跑 vue-tsc）
3. commit `feat(hotlist): add frontend api and routing`

---

## T5: Hotlist.vue 页面（frontend-engineer）

**Files**:
- Create: `G:\Orbit\frontend\src\views\Hotlist.vue`

**要求**:
- 参照 `Reading.vue` 的结构（el-card 容器 + 状态管理 + API 调用），UI 遵循 `G:\Orbit\design-system\orbit\MASTER.md` Glass Morphism 风格（读该文件！浅深双模式用现有 data-theme 变量）
- 页面元素：
  1. 标题「热榜」+ 数据源标签（目前固定 GitHub Trending，数据从 `/sources/` 接口取）
  2. `el-date-picker`（value-format="YYYY-MM-DD"）选择日期，默认今天，切换时重新拉取
  3. 「立即抓取」按钮 → `hotlistApi.fetch()` → 成功后刷新列表（显示抓取数量 message）
  4. 热榜列表：排名徽章（1-3 名用特殊颜色）、仓库名（可点击外链，target=_blank）、描述、语言标签、⭐ 今日/总数、🍴 fork 数
  5. 空态：当天无数据时显示提示「当天暂无热榜数据，点击立即抓取获取」
  6. loading 状态（el-skeleton 或 v-loading）
- 列表用响应式布局，条目间留白，玻璃卡片风格

**验收**:
1. 文件存在且能通过 `npx vite build`
2. 浏览器手动验证（browser-harness）：登录 → 侧边栏点「热榜」→ 页面显示数据（先点「立即抓取」）→ 排名/描述/star 可见 → 切日期空态正常
3. commit `feat(hotlist): add Hotlist page`

---

## T6: QA 端到端验收（qa-engineer）

**依赖**: T1-T5 全部完成。

**验收清单**（全部实测，输出证据）:
1. 后端启动正常（`venv\Scripts\python.exe -m uvicorn app.main:app --port 8000`）
2. JWT 登录后：fetch → list → sources 三端点全通，数据真实（跟 github.com/trending 对照 ≥5 条）
3. 边界：无 token 401；不存在的 source 400；重复 fetch 幂等（count=0）
4. 前端 `npx vite build` 通过；`npm run dev` 或已打包页面打开 `/hotlist`，浏览器截图证明列表渲染（截图存 `G:\Orbit\qa_shots\`）
5. 全链路：页面点「立即抓取」→ 数据出现 → 刷新后仍在（DB 持久化）
6. 输出 QA 报告到 `G:\Orbit\docs\qa-hotlist.md`，列出通过项/问题项
